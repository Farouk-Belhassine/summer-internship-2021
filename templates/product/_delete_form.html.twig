<form method="post" action="{{ path('product_delete', {'id': product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="fa fa-cogs" type="button"><a href="{{ path('product_edit', {'id': product.id}) }}"> Edit</a></button>
    <button type="submit" class="fa fa-trash"> Delete</button>
</form>