{% extends 'base.html.twig' %}

{% block title %}ProdSteps{% endblock %}

{% block body %}
    <h1>ProdSteps</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prod_step.id }}</td>
            </tr>
            <tr>
                <th>Descp</th>
                <td>{{ prod_step.descp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prod_steps_index') }}">back to list</a>

    <a href="{{ path('prod_steps_edit', {'id': prod_step.id}) }}">edit</a>

    {{ include('prod_steps/_delete_form.html.twig') }}
{% endblock %}
